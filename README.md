# tfpy

A personal Python package for processing toroidal field. 

## Installation

Install the package from the repository root with pip:

```bash
python -m pip install .
```

For development, use editable mode so local source changes are picked up without reinstalling:

```bash
python -m pip install -e .
```

